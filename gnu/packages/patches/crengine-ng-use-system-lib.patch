diff --git a/CMakeLists.txt b/CMakeLists.txt
index ca11d45c..f201f92c 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -386,8 +386,17 @@ else()
 endif()

 # nanosvg library
-if(USE_NANOSVG)
-    list(APPEND PRIVATE_INCLUDE_DIRECTORIES ${CMAKE_SOURCE_DIR}/thirdparty/nanosvg/src)
+if(WITH_NANOSVG STREQUAL "AUTO")
+    find_package(nanosvg)
+elseif(WITH_NANOSVG)
+    find_package(nanosvg REQUIRED)
+endif()
+if (NANOSVG_FOUND)
+    set(USE_NANOSVG ON)
+    list(APPEND PRIVATE_INCLUDE_DIRECTORIES ${NANOSVG_INCLUDE_DIRS})
+    list(APPEND PRIVATE_LINK_LIBRARIES ${NANOSVG_LIBRARIES})
+else()
+    set(USE_NANOSVG OFF)
 endif()

 # patched built-in libchm
@@ -445,13 +454,19 @@ if (USE_CMARK_GFM)
     list(APPEND PRIVATE_LINK_LIBRARIES cre_cmark-gfm-extensions)
 endif(USE_CMARK_GFM)

-# built-in md4c
-if (USE_MD4C)
-    message("Will build md4c library")
-    add_subdirectory(thirdparty/md4c)
-    list(APPEND PRIVATE_INCLUDE_DIRECTORIES ${CMAKE_SOURCE_DIR}/thirdparty/md4c/src)
-    list(APPEND PRIVATE_LINK_LIBRARIES cre_md4c-html)
-endif(USE_MD4C)
+# md4c library
+if(WITH_MD4C STREQUAL "AUTO")
+    find_package(md4c)
+elseif(WITH_MD4C)
+    find_package(md4c REQUIRED)
+endif()
+if (MD4C_FOUND)
+    set(USE_MD4C ON)
+    list(APPEND PRIVATE_INCLUDE_DIRECTORIES ${MD4C_INCLUDE_DIRS})
+    list(APPEND PRIVATE_LINK_LIBRARIES ${MD4C_LIBRARIES})
+else()
+    set(USE_MD4C OFF)
+endif()

 # check usage consistency
 if (NOT USE_FREETYPE)
