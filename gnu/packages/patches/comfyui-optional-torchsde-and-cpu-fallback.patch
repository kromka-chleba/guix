diff --git a/comfy/k_diffusion/sampling.py b/comfy/k_diffusion/sampling.py
index 7ea0af2..f7ec5b7 100644
--- a/comfy/k_diffusion/sampling.py
+++ b/comfy/k_diffusion/sampling.py
@@ -1,4 +1,7 @@
-import torchsde
+try:
+    import torchsde
+except ImportError:
+    torchsde = None
 import torch
 from torch import nn
 from torchdiffeq import odeint
@@ -104,6 +107,8 @@ class BatchedBrownianTree:
     """A wrapper around torchsde.BrownianTree that enables batches of entropy."""
 
     def __init__(self, x, t0, t1, seed=None, **kwargs):
+        if torchsde is None:
+            raise RuntimeError("torchsde is required for this sampler; install torchsde to use BrownianTree-based samplers.")
         t0, t1, self.sign = self.sort(t0, t1)
         w0 = kwargs.get('w0', torch.zeros_like(x))
         if seed is None:
diff --git a/comfy/model_management.py b/comfy/model_management.py
index 38d8fbf..01746ac 100644
--- a/comfy/model_management.py
+++ b/comfy/model_management.py
@@ -120,7 +120,11 @@ def get_torch_device():
     global cpu_state
     if directml_enabled:
         return directml_device
-    return torch.device(torch.cuda.current_device())
+    if torch.cuda.is_available():
+        return torch.device(torch.cuda.current_device())
+
+    cpu_state = CPUState.CPU
+    return torch.device("cpu")
 
 def get_total_memory(dev=None, torch_total_too=False):
     global directml_enabled
